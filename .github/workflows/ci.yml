name: Swift
on: [push]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: set -o pipefail && xcodebuild -project "Magnetic.xcodeproj" -scheme "Example" -configuration Debug ONLY_ACTIVE_ARCH=NO clean build | xcpretty
