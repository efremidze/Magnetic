name: Swift
on: [push]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: set -o pipefail && xcodebuild -project "$PROJECT" -scheme "$EXAMPLE_SCHEME" clean build | xcpretty;
